I don't understand at all, but there are some cases that someone accidentally merge a pull request without actually clicking its merge button.
So I just created a repo to experiment if such things would possibly happen.
